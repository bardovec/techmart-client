import {makeStyles} from '@material-ui/core';

export default makeStyles(() => ({
    descriptions: {
        textAlign: 'center',
    },
    productDetailsWrapper: {
        width: '100%',
        marginBottom: '30px',
        borderRadius: '10px',
    }
}))